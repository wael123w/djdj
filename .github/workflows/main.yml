name: Build Laravel Vendor

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Extract project ZIP
        run: |
          unzip -q alfaore-store.zip -d extracted

      - name: Show extracted files
        run: |
          ls -la extracted

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          tools: composer:v2
          extensions: mbstring, bcmath, ctype, fileinfo, openssl, pdo, pdo_mysql, tokenizer, xml

      - name: Verify Composer
        working-directory: extracted
        run: |
          php -v
          composer --version
          test -f composer.json

      - name: Install Composer dependencies
        working-directory: extracted
        env:
          COMPOSER_ALLOW_SUPERUSER: 1
        run: |
          composer install --no-dev --optimize-autoloader --no-interaction

      - name: Create vendor ZIP
        working-directory: extracted
        run: |
          zip -r vendor.zip vendor

      - name: Upload vendor ZIP
        uses: actions/upload-artifact@v4
        with:
          name: vendor-folder
          path: extracted/vendor.zip
